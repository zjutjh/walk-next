# 复制此文件到apps\admin文件夹，重命名为.env；在VITE_HOST=后面填写服务器地址

# 服务器的URL（不要遗漏开头协议名，末尾斜杠可有可无，不要带/api）
VITE_HOST=

# 是否显示地图选点器热区边界，true为是，其余值均为否（调试预览用，仅在vite development环境生效，生产环境不会显示）
VITE_DEBUG_SHOW_HOT_RECT_BORDER=true
